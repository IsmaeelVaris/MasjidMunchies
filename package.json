{
  "name": "masjidmunchies",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "devDependencies": {
    "@types/express": "^4.17.23",
    "@types/node": "^24.3.0",
    "eslint": "^9.0.0",
    "rimraf": "^5.0.7",
    "ts-node-dev": "^2.0.0",
    "turbo": "^1.10.0",
    "typescript": "^4.9.5"
  },
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "dependencies": {
    "express": "^5.1.0"
  }
}

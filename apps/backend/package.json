{
  "name": "backend",
  "private": true,
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.10",
    "typescript": "^4.9.5"
  },
"scripts": {
  "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
  "build": "tsc -b",
  "typecheck": "tsc --noEmit",
  "clean": "rimraf dist"
}

}
